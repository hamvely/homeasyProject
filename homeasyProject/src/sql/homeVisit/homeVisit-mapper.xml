<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

	<entry key="selectHomeVisit">
		 SELECT
		        USER_FILE_RENAME
		       , NICKNAME
		       , POST_FILE_RENAME
		       , POST_CONTENT
		       , PCOM_CONTENT
		  FROM MEMBER M
		  JOIN POST_FILE ON ( M.USER_NO = POST_FILE.POST_NO)
		  JOIN POST ON (M.USER_NO = POST.USER_NO)
		  JOIN POST_COMMENT ON ( M.USER_NO = POST_COMMENT.USER_NO)
		 WHERE M.USER_STATUS = 'Y'
		   AND POST_CONTENT LIKE '집들이%'
	</entry>
	
	
	<entry	key="selectReplyList">
		SELECT
			  NICKNAME
			, PCOM_CONTENT
		 FROM MEMBER M
		 JOIN POST_COMMENT ON (M.USER_NO = POST_COMMENT.USER_NO)
	</entry>



	<entry key="adminHomeVisitList">
		SELECT
		      POST_NO
		    , EMAIL
		    , POST_TITLE
		    , POST_CREATE_DATE
		    , POST_COUNT
		FROM POST P
		JOIN MEMBER ON (P.USER_NO = MEMBER.USER_NO)
		WHERE POST_STATUS = 'Y'
		  AND POST_TITLE LIKE '집들이%'
	</entry>

</properties>